"""Screen modules for the setter dashboard, one per `st.Page` (see setter/app.py).

Each module exposes a single `render()` function with no arguments -- the
navigation shell in `setter/app.py` is what decides which one runs."""
